<!--android-docs
{"v":2,"id":"mtiakglh","docKey":"Nguyễn Minh Tùng - Android - CV.pdf::133175","doc":"Nguyễn Minh Tùng - Android - CV.pdf","page":1,"ts":1789146501778,"quote":"Kotlin","xr":0.36039323136613177,"yr":0.23242119847592657,"ord":4}
-->

# Kotlin

> Nguyễn Minh Tùng - Android - CV.pdf · trang 1 · 00:08:21 12/9/2026

<!--body-->

<!--rte--><p></p><div><div class="rte-menu"><div class="rte-menu-h" data-open="0"><span style="font-weight: bold;"><span style="font-size: 20px;"><span style="font-size: 20px;"><span style="font-size: 15px;"><span style="font-size: 15px;"><span style="font-size: 18px;"><span style="font-size: 18px;"><a href="https://kotlinlang.org/docs/data-classes.html" class="ext-link" target="_blank" rel="noopener">Data classes</a></span></span></span></span></span></span></span></div><div class="rte-menu-b"><div><pre class="rte-code" data-lang="plain" style="font-size: 13px; color: rgb(226, 232, 240);"><code>Trong Kotlin, <span style="color: rgb(244, 151, 1);">class</span> là khai báo lớp thông thường,&nbsp;
còn <span style="color: rgb(244, 151, 1);">data class</span> là một loại <span style="color: rgb(244, 151, 1);">class</span> đặc biệt được thiết kế để chứa dữ liệu,&nbsp;
và trình biên dịch sẽ tự động sinh ra một số hàm hữu ích cho nó.
<span style="color: rgb(244, 151, 1); font-weight: bold;"><span style="font-size: 15px;">equals()/hashCode()</span></span> dựa trên các thuộc tính khai báo trong constructor chính
<span style="color: rgb(244, 151, 1); font-weight: bold;"><span style="font-size: 15px;">toString()</span></span> dạng dễ đọc
<span style="color: rgb(244, 151, 1); font-weight: bold;"><span style="font-size: 15px;">copy()</span></span> để tạo bản sao có sửa đổi một vài field
<span style="color: rgb(244, 151, 1); font-weight: bold;"><span style="font-size: 15px;">componentN()</span></span> để hỗ trợ <a href="https://kotlinlang.org/docs/destructuring-declarations.html" class="ext-link" target="_blank" rel="noopener">destructuring declaration</a><br><br>Một vài điểm cần lưu ý: <span style="color: rgb(244, 151, 1);">data class</span> phải có ít nhất một tham số trong <span style="color: rgb(244, 151, 1);">constructor</span> chính, và các tham số phải là <code style="color: rgb(244, 151, 1);">val</code> hoặc <code style="color: rgb(244, 151, 1);">var</code>. <span style="color: rgb(244, 151, 1);">Data class</span> không thể là <code style="color: rgb(244, 151, 1);">abstract</code>, <code style="color: rgb(244, 151, 1);">open</code>, <code style="color: rgb(244, 151, 1);">sealed</code> hay <code style="color: rgb(244, 151, 1);">inner</code>. Nếu <span style="color: rgb(244, 151, 1);">override</span> thủ công <code style="color: rgb(244, 151, 1);">equals()</code>, <code style="color: rgb(244, 151, 1);">hashCode()</code>, hoặc <code style="color: rgb(244, 151, 1);">toString()</code>, <span style="color: rgb(244, 151, 1);">Kotlin</span> sẽ dùng bản viết thay vì bản tự sinh.</code></pre></div><div><br></div><div><div style="color: rgb(226, 232, 240); white-space: pre-wrap; background-color: rgb(15, 15, 20);"></div><pre class="rte-code" data-lang="kotlin"><code><span class="hl-com">//componentN():</span>

<span class="hl-kw">data</span> <span class="hl-kw">class</span> <span class="hl-fn">Person</span><span class="hl-punct">(</span><span class="hl-kw">val</span> name<span class="hl-punct">:</span> <span class="hl-type">String</span><span class="hl-punct">,</span> <span class="hl-kw">val</span> age<span class="hl-punct">:</span> <span class="hl-type">Int</span><span class="hl-punct">)</span>

<span class="hl-kw">val</span> p <span class="hl-punct">=</span> <span class="hl-fn">Person</span><span class="hl-punct">(</span><span class="hl-str">"An"</span><span class="hl-punct">,</span> <span class="hl-num">25</span><span class="hl-punct">)</span>

<span class="hl-fn">println</span><span class="hl-punct">(</span>p<span class="hl-punct">.</span><span class="hl-fn">component1</span><span class="hl-punct">(</span><span class="hl-punct">)</span><span class="hl-punct">)</span>&nbsp; <span class="hl-com">// "An"</span>
<span class="hl-fn">println</span><span class="hl-punct">(</span>p<span class="hl-punct">.</span><span class="hl-fn">component2</span><span class="hl-punct">(</span><span class="hl-punct">)</span><span class="hl-punct">)</span>&nbsp; <span class="hl-com">// 25</span>

<span class="hl-kw">val</span> <span class="hl-punct">(</span>name<span class="hl-punct">,</span> age<span class="hl-punct">)</span> <span class="hl-punct">=</span> p&nbsp;
<span class="hl-fn">println</span><span class="hl-punct">(</span>name<span class="hl-punct">)</span>  <span class="hl-com">// "An"&nbsp;</span>
<span class="hl-fn">println</span><span class="hl-punct">(</span>age<span class="hl-punct">)</span>   <span class="hl-com">// 25</span>
<span class="hl-com">//Ở đây, Kotlin ngầm hiểu name = p.component1() và age = p.component2()</span>

<span class="hl-kw">val</span> people <span class="hl-punct">=</span> <span class="hl-fn">listOf</span><span class="hl-punct">(</span><span class="hl-fn">Person</span><span class="hl-punct">(</span><span class="hl-str">"An"</span><span class="hl-punct">,</span> <span class="hl-num">25</span><span class="hl-punct">)</span><span class="hl-punct">,</span> <span class="hl-fn">Person</span><span class="hl-punct">(</span><span class="hl-str">"Bình"</span><span class="hl-punct">,</span> <span class="hl-num">30</span><span class="hl-punct">)</span><span class="hl-punct">)</span>

<span class="hl-kw">for</span> <span class="hl-punct">(</span><span class="hl-punct">(</span>name<span class="hl-punct">,</span> age<span class="hl-punct">)</span> <span class="hl-kw">in</span> people<span class="hl-punct">)</span> <span class="hl-punct">{</span>
    <span class="hl-fn">println</span><span class="hl-punct">(</span><span class="hl-str">"$name là $age tuổi"</span><span class="hl-punct">)</span>
<span class="hl-punct">}</span>

<span class="hl-com">// Với Map cũng destructure được vì Map.Entry có sẵn component1()/component2()</span>
<span class="hl-kw">val</span> map <span class="hl-punct">=</span> <span class="hl-fn">mapOf</span><span class="hl-punct">(</span><span class="hl-str">"a"</span> to <span class="hl-num">1</span><span class="hl-punct">,</span> <span class="hl-str">"b"</span> to <span class="hl-num">2</span><span class="hl-punct">)</span>
<span class="hl-kw">for</span> <span class="hl-punct">(</span><span class="hl-punct">(</span>key<span class="hl-punct">,</span> value<span class="hl-punct">)</span> <span class="hl-kw">in</span> map<span class="hl-punct">)</span> <span class="hl-punct">{</span>
    <span class="hl-fn">println</span><span class="hl-punct">(</span><span class="hl-str">"$key -&gt; $value"</span><span class="hl-punct">)</span>
<span class="hl-punct">}</span></code></pre><div><br></div><div style="color: rgb(226, 232, 240); white-space: pre-wrap; background-color: rgb(15, 15, 20);"></div></div></div></div></div><div><br></div><div></div><div><span style="font-size: 18px; font-weight: 700;"><a href="https://kotlinlang.org/docs/sealed-classes.html" class="ext-link" target="_blank" rel="noopener"></a></span><div class="rte-menu"><div class="rte-menu-h" data-open="0"><span style="font-size: 18px; font-weight: 700;"><a href="https://kotlinlang.org/docs/sealed-classes.html" class="ext-link" target="_blank" rel="noopener">Sealed (classes và interfaces)</a>&nbsp;-&nbsp;</span><span style="font-size: 18px; font-weight: bold; font-family: -apple-system, BlinkMacSystemFont, &quot;Segoe UI&quot;, sans-serif;"><a href="https://kotlinlang.org/docs/enum-classes.html" class="ext-link" target="_blank" rel="noopener">Enum</a></span></div><div class="rte-menu-b"><div><div style="color: rgb(226, 232, 240);"><pre class="rte-code" data-lang="text"><code><span style="color: rgb(244, 151, 1);">enum class</span> mỗi giá trị là một instance cố định, không mang được nhiều dữ liệu khác kiểu nhau<br><span style="color: rgb(244, 151, 1);">sealed class/sealed interface</span> mỗi "nhánh con" có thể là một <span style="color: rgb(244, 151, 1);">class</span> riêng với dữ liệu và cấu trúc hoàn toàn khác nhau<br><br>Enum:<br><pre class="rte-code" data-lang="kotlin"><code><span class="hl-kw">enum</span> <span class="hl-kw">class</span> <span class="hl-type">Direction</span> <span class="hl-punct">{</span>
    <span class="hl-type">NORTH</span><span class="hl-punct">,</span> <span class="hl-type">SOUTH</span><span class="hl-punct">,</span> <span class="hl-type">WEST</span><span class="hl-punct">,</span> <span class="hl-type">EAST</span>
<span class="hl-punct">}</span>

<span class="hl-kw">enum</span> <span class="hl-kw">class</span> <span class="hl-fn">Color</span><span class="hl-punct">(</span><span class="hl-kw">val</span> rgb<span class="hl-punct">:</span> <span class="hl-type">Int</span><span class="hl-punct">)</span> <span class="hl-punct">{</span>
    <span class="hl-fn">RED</span><span class="hl-punct">(</span>xFF0000<span class="hl-punct">)</span><span class="hl-punct">,</span>
    <span class="hl-fn">GREEN</span><span class="hl-punct">(</span>x00FF00<span class="hl-punct">)</span><span class="hl-punct">,</span>
    <span class="hl-fn">BLUE</span><span class="hl-punct">(</span>x0000FF<span class="hl-punct">)</span>
<span class="hl-punct">}</span></code></pre><div><br></div>Sealed:<br><span style="color: rgb(204, 123, 244);"></span><pre class="rte-code" data-lang="kotlin"><span class="hl-com">// Create a sealed interface</span>
<span class="hl-kw">sealed</span> <span class="hl-kw">interface</span> <span class="hl-type">Error</span>

<span class="hl-com">// Create a sealed class that implements sealed interface Error</span>
<span class="hl-kw">sealed</span> <span class="hl-kw">class</span> <span class="hl-fn">IOError</span><span class="hl-punct">(</span><span class="hl-punct">)</span><span class="hl-punct">:</span> <span class="hl-type">Error</span>

<span class="hl-com">// Define subclasses that extend sealed class 'IOError'</span>
<span class="hl-kw">class</span> <span class="hl-fn">FileReadError</span><span class="hl-punct">(</span><span class="hl-kw">val</span> file<span class="hl-punct">:</span> <span class="hl-type">File</span><span class="hl-punct">)</span><span class="hl-punct">:</span> <span class="hl-fn">IOError</span><span class="hl-punct">(</span><span class="hl-punct">)</span>
<span class="hl-kw">class</span> <span class="hl-fn">DatabaseError</span><span class="hl-punct">(</span><span class="hl-kw">val</span> source<span class="hl-punct">:</span> <span class="hl-type">DataSource</span><span class="hl-punct">)</span><span class="hl-punct">:</span> <span class="hl-fn">IOError</span><span class="hl-punct">(</span><span class="hl-punct">)</span>

<span class="hl-com">// Create a singleton object implementing the 'Error' sealed interface</span>
<span class="hl-kw">object</span> <span class="hl-type">RuntimeError</span> <span class="hl-punct">:</span> <span class="hl-type">Error</span>

<span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span>

<span class="hl-kw">sealed</span> <span class="hl-kw">class</span> <span class="hl-type">Payment</span> <span class="hl-punct">{</span>
    <span class="hl-kw">data</span> <span class="hl-kw">class</span> <span class="hl-fn">CreditCard</span><span class="hl-punct">(</span><span class="hl-kw">val</span> number<span class="hl-punct">:</span> <span class="hl-type">String</span><span class="hl-punct">,</span> <span class="hl-kw">val</span> expiryDate<span class="hl-punct">:</span> <span class="hl-type">String</span><span class="hl-punct">)</span> <span class="hl-punct">:</span> <span class="hl-fn">Payment</span><span class="hl-punct">(</span><span class="hl-punct">)</span>
    <span class="hl-kw">data</span> <span class="hl-kw">class</span> <span class="hl-fn">PayPal</span><span class="hl-punct">(</span><span class="hl-kw">val</span> email<span class="hl-punct">:</span> <span class="hl-type">String</span><span class="hl-punct">)</span> <span class="hl-punct">:</span> <span class="hl-fn">Payment</span><span class="hl-punct">(</span><span class="hl-punct">)</span>
    <span class="hl-kw">data</span> <span class="hl-kw">object</span> <span class="hl-type">Cash</span> <span class="hl-punct">:</span> <span class="hl-fn">Payment</span><span class="hl-punct">(</span><span class="hl-punct">)</span>
<span class="hl-punct">}</span>

<span class="hl-kw">fun</span> <span class="hl-fn">processPayment</span><span class="hl-punct">(</span>payment<span class="hl-punct">:</span> <span class="hl-type">Payment</span><span class="hl-punct">)</span> <span class="hl-punct">{</span>
    <span class="hl-kw">when</span> <span class="hl-punct">(</span>payment<span class="hl-punct">)</span> <span class="hl-punct">{</span>
        <span class="hl-kw">is</span> <span class="hl-type">Payment</span><span class="hl-punct">.</span><span class="hl-type">CreditCard</span> <span class="hl-punct">-</span><span class="hl-punct">&gt;</span> <span class="hl-fn">processCreditCardPayment</span><span class="hl-punct">(</span>payment<span class="hl-punct">.</span>number<span class="hl-punct">,</span> payment<span class="hl-punct">.</span>expiryDate<span class="hl-punct">)</span>
        <span class="hl-kw">is</span> <span class="hl-type">Payment</span><span class="hl-punct">.</span><span class="hl-type">PayPal</span> <span class="hl-punct">-</span><span class="hl-punct">&gt;</span> <span class="hl-fn">processPayPalPayment</span><span class="hl-punct">(</span>payment<span class="hl-punct">.</span>email<span class="hl-punct">)</span>
        <span class="hl-kw">is</span> <span class="hl-type">Payment</span><span class="hl-punct">.</span><span class="hl-type">Cash</span> <span class="hl-punct">-</span><span class="hl-punct">&gt;</span> <span class="hl-fn">processCashPayment</span><span class="hl-punct">(</span><span class="hl-punct">)</span>
    <span class="hl-punct">}</span>
<span class="hl-punct">}</span><div><br></div><div><br></div><div><br></div><div><br></div></pre></code></pre></div><div style="color: rgb(226, 232, 240);"><code class="code" id="h386he_69" style="padding: 2px 6px; background: rgba(25, 25, 28, 0.05); border-radius: 3px; font-family: &quot;JetBrains Mono&quot;, SFMono-Regular, Consolas, &quot;Liberation Mono&quot;, Menlo, Courier, monospace; font-size: 15px; border: 0px; font-variant-ligatures: none; font-variant-numeric: inherit; font-variant-east-asian: inherit; font-variant-alternates: inherit; font-variant-position: inherit; font-variant-emoji: inherit; font-stretch: inherit; line-height: inherit; font-optical-sizing: inherit; font-size-adjust: inherit; font-kerning: inherit; font-feature-settings: inherit; font-variation-settings: inherit; font-language-override: inherit; vertical-align: baseline; display: inline; word-break: break-word; color: rgb(25, 25, 28); letter-spacing: 0.024px;"></code><pre class="rte-code" data-lang="text"><code>enum classes can't extend a <span style="color: rgb(244, 151, 1);">sealed class</span>, or any other class. <br>However, they can implement <span style="color: rgb(244, 151, 1);">sealed interfaces</span>:<br><span class="prism--token prism--keyword" style="border: 0px; font-variant-ligatures: none; font-variant-numeric: inherit; font-variant-east-asian: inherit; font-variant-alternates: inherit; font-variant-position: inherit; font-variant-emoji: inherit; font-stretch: inherit; font-size: 15.2px; line-height: inherit; font-family: &quot;JetBrains Mono&quot;, SFMono-Regular, Consolas, &quot;Liberation Mono&quot;, Menlo, Courier, monospace; font-optical-sizing: inherit; font-size-adjust: inherit; font-kerning: inherit; font-feature-settings: inherit; font-variation-settings: inherit; font-language-override: inherit; vertical-align: baseline; color: rgb(0, 51, 179); letter-spacing: 0.024px; white-space: pre; background-color: rgba(25, 25, 28, 0.05);"></span><pre class="rte-code" data-lang="kotlin"><code><span class="hl-kw">sealed</span> <span class="hl-kw">interface</span> <span class="hl-type">Error</span>

<span class="hl-com">// enum class extending the sealed interface Error</span>
<span class="hl-kw">enum</span> <span class="hl-kw">class</span> <span class="hl-type">ErrorType</span> <span class="hl-punct">:</span> <span class="hl-type">Error</span> <span class="hl-punct">{</span>
    <span class="hl-type">FILE_ERROR</span><span class="hl-punct">,</span> <span class="hl-type">DATABASE_ERROR</span>
<span class="hl-punct">}</span></code><div><code><br></code></div></pre></code></pre></div></div></div></div></div><div><div><br></div></div><div><div class="rte-menu"><div class="rte-menu-h" data-open="0"><span style="font-weight: bold;"><span style="font-size: 20px;"><span style="font-size: 15px;"><span style="font-size: 18px;"><a href="https://kotlinlang.org/docs/lambdas.html#higher-order-functions" class="ext-link" target="_blank" rel="noopener">Higher-order functions</a></span></span></span></span></div><div class="rte-menu-b"><p data-pm-slice="0 0 []">A higher-order function is a function that takes functions as parameters, or returns a function.</p><p data-pm-slice="0 0 []"><br></p><p data-pm-slice="0 0 []"><span class="prism--token prism--keyword" style="border: 0px; font-variant-ligatures: none; font-variant-numeric: inherit; font-variant-east-asian: inherit; font-variant-alternates: inherit; font-variant-position: inherit; font-variant-emoji: inherit; font-stretch: inherit; font-size: 15.2px; line-height: inherit; font-family: &quot;JetBrains Mono&quot;, SFMono-Regular, Consolas, &quot;Liberation Mono&quot;, Menlo, Courier, monospace; font-optical-sizing: inherit; font-size-adjust: inherit; font-kerning: inherit; font-feature-settings: inherit; font-variation-settings: inherit; font-language-override: inherit; vertical-align: baseline; color: rgb(0, 51, 179); letter-spacing: 0.024px; white-space: pre; background-color: rgba(25, 25, 28, 0.05);"></span></p><pre class="rte-code" data-lang="kotlin"><code><span class="hl-kw">fun</span> <span class="hl-punct">&lt;</span><span class="hl-type">T</span><span class="hl-punct">,</span> <span class="hl-type">R</span><span class="hl-punct">&gt;</span> <span class="hl-type">Collection</span><span class="hl-punct">&lt;</span><span class="hl-type">T</span><span class="hl-punct">&gt;</span><span class="hl-punct">.</span><span class="hl-fn">fold</span><span class="hl-punct">(</span>
    initial<span class="hl-punct">:</span> <span class="hl-type">R</span><span class="hl-punct">,</span>
    combine<span class="hl-punct">:</span> <span class="hl-punct">(</span>acc<span class="hl-punct">:</span> <span class="hl-type">R</span><span class="hl-punct">,</span> nextElement<span class="hl-punct">:</span> <span class="hl-type">T</span><span class="hl-punct">)</span> <span class="hl-punct">-</span><span class="hl-punct">&gt;</span> <span class="hl-type">R</span>
<span class="hl-punct">)</span><span class="hl-punct">:</span> <span class="hl-type">R</span> <span class="hl-punct">{</span>
    <span class="hl-kw">var</span> accumulator<span class="hl-punct">:</span> <span class="hl-type">R</span> <span class="hl-punct">=</span> initial
    <span class="hl-kw">for</span> <span class="hl-punct">(</span>element<span class="hl-punct">:</span> <span class="hl-type">T</span> <span class="hl-kw">in</span> <span class="hl-kw">this</span><span class="hl-punct">)</span> <span class="hl-punct">{</span>
        accumulator <span class="hl-punct">=</span> <span class="hl-fn">combine</span><span class="hl-punct">(</span>accumulator<span class="hl-punct">,</span> element<span class="hl-punct">)</span>
    <span class="hl-punct">}</span>
    <span class="hl-kw">return</span> accumulator
<span class="hl-punct">}</span>

<span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span><span class="hl-punct">=</span>

<span class="hl-kw">val</span> items <span class="hl-punct">=</span> <span class="hl-fn">listOf</span><span class="hl-punct">(</span><span class="hl-num">1</span><span class="hl-punct">,</span> <span class="hl-num">2</span><span class="hl-punct">,</span> <span class="hl-num">3</span><span class="hl-punct">,</span> <span class="hl-num">4</span><span class="hl-punct">,</span> <span class="hl-num">5</span><span class="hl-punct">)</span>

<span class="hl-com">// Lambdas are code blocks enclosed in curly braces.</span>
items<span class="hl-punct">.</span><span class="hl-fn">fold</span><span class="hl-punct">(</span><span class="hl-num">0</span><span class="hl-punct">,</span> <span class="hl-punct">{</span> 
    <span class="hl-com">// When a lambda has parameters, they go first, followed by '-&gt;'</span>
    acc<span class="hl-punct">:</span> <span class="hl-type">Int</span><span class="hl-punct">,</span> i<span class="hl-punct">:</span> <span class="hl-type">Int</span> <span class="hl-punct">-</span><span class="hl-punct">&gt;</span> 
    <span class="hl-fn">print</span><span class="hl-punct">(</span><span class="hl-str">"acc = $acc, i = $i, "</span><span class="hl-punct">)</span> 
    <span class="hl-kw">val</span> result <span class="hl-punct">=</span> acc <span class="hl-punct">+</span> i
    <span class="hl-fn">println</span><span class="hl-punct">(</span><span class="hl-str">"result = $result"</span><span class="hl-punct">)</span>
    <span class="hl-com">// The last expression in a lambda is considered the return value:</span>
    result
<span class="hl-punct">}</span><span class="hl-punct">)</span>

<span class="hl-com">// Parameter types in a lambda are optional if they can be inferred:</span>
<span class="hl-kw">val</span> joinedToString <span class="hl-punct">=</span> items<span class="hl-punct">.</span><span class="hl-fn">fold</span><span class="hl-punct">(</span><span class="hl-str">"Elements:"</span><span class="hl-punct">,</span> <span class="hl-punct">{</span> acc<span class="hl-punct">,</span> i <span class="hl-punct">-</span><span class="hl-punct">&gt;</span> acc <span class="hl-punct">+</span> <span class="hl-str">" "</span> <span class="hl-punct">+</span> i <span class="hl-punct">}</span><span class="hl-punct">)</span>

<span class="hl-com">// Function references can also be used for higher-order function calls:</span>
<span class="hl-kw">val</span> product <span class="hl-punct">=</span> items<span class="hl-punct">.</span><span class="hl-fn">fold</span><span class="hl-punct">(</span><span class="hl-num">1</span><span class="hl-punct">,</span> <span class="hl-type">Int</span><span class="hl-punct">:</span><span class="hl-punct">:</span>times<span class="hl-punct">)</span></code></pre><div><br></div><div><p data-pm-slice="0 0 []"><a href="https://kotlinlang.org/docs/lambdas.html#function-types" class="ext-link" target="_blank" rel="noopener">Function types﻿</a></p></div><div><p data-pm-slice="0 0 []">Kotlin uses function types, such as <code>(Int) -&gt; String</code>, for declarations that deal with functions: <code>val onClick: () -&gt; Unit =, ...</code></p></div><div><br><p data-pm-slice="0 0 []"><a href="https://kotlinlang.org/docs/lambdas.html#instantiating-a-function-type" class="ext-link" target="_blank" rel="noopener">Instantiating a function type</a></p><ul data-pm-slice="0 0 []"><li><p>a <a href="https://kotlinlang.org/docs/lambdas.html#lambda-expressions-and-anonymous-functions" class="ext-link" target="_blank" rel="noopener">lambda expression:</a> <code>{ a, b -&gt; a + b }</code>,</p></li><li><p>an <a target="_blank" rel="noopener" class="link ext-link" href="https://kotlinlang.org/docs/lambdas.html#anonymous-functions">anonymous function</a>: <code>fun(s: String): Int { return s.toIntOrNull() ?: 0 }</code></p></li><li><p><a target="_blank" rel="noopener" class="link ext-link" href="https://kotlinlang.org/docs/lambdas.html#function-literals-with-receiver" style="font-family: -apple-system, BlinkMacSystemFont, &quot;Segoe UI&quot;, sans-serif;">Function literals with receiver</a><span style="font-family: -apple-system, BlinkMacSystemFont, &quot;Segoe UI&quot;, sans-serif;"> can be used as values of function types with receiver.</span></p></li></ul><ul data-pm-slice="0 0 []"><li><p>a top-level, local, member, or extension <a target="_blank" rel="noopener" class="link ext-link" href="https://kotlinlang.org/docs/reflection.html#function-references">function</a>: <code>::isOdd</code>, <code>String::toInt</code>,</p></li><li><p>a top-level, member, or extension <a target="_blank" rel="noopener" class="link ext-link" href="https://kotlinlang.org/docs/reflection.html#property-references">property</a>: <code>List&lt;Int&gt;::size</code>,</p></li><li><p>a <a target="_blank" rel="noopener" class="link ext-link" href="https://kotlinlang.org/docs/reflection.html#constructor-references">constructor</a>: <code>::Regex</code></p></li></ul></div><div><p data-pm-slice="0 0 []">Use instances of a custom class that implements a function type as an interface:</p><p data-pm-slice="0 0 []"><span class="prism--token prism--keyword" style="border: 0px; font-variant-ligatures: none; font-variant-numeric: inherit; font-variant-east-asian: inherit; font-variant-alternates: inherit; font-variant-position: inherit; font-variant-emoji: inherit; font-stretch: inherit; font-size: 15.2px; line-height: inherit; font-family: &quot;JetBrains Mono&quot;, SFMono-Regular, Consolas, &quot;Liberation Mono&quot;, Menlo, Courier, monospace; font-optical-sizing: inherit; font-size-adjust: inherit; font-kerning: inherit; font-feature-settings: inherit; font-variation-settings: inherit; font-language-override: inherit; vertical-align: baseline; color: rgb(0, 51, 179); letter-spacing: 0.024px; white-space: pre; background-color: rgba(25, 25, 28, 0.05);"></span></p><pre class="rte-code" data-lang="kotlin"><code><span class="hl-kw">class</span> <span class="hl-type">IntTransformer</span><span class="hl-punct">:</span> <span class="hl-punct">(</span><span class="hl-type">Int</span><span class="hl-punct">)</span> <span class="hl-punct">-</span><span class="hl-punct">&gt;</span> <span class="hl-type">Int</span> <span class="hl-punct">{</span>
    <span class="hl-kw">override</span> <span class="hl-kw">operator</span> <span class="hl-kw">fun</span> <span class="hl-fn">invoke</span><span class="hl-punct">(</span>x<span class="hl-punct">:</span> <span class="hl-type">Int</span><span class="hl-punct">)</span><span class="hl-punct">:</span> <span class="hl-type">Int</span> <span class="hl-punct">=</span> <span class="hl-fn">TODO</span><span class="hl-punct">(</span><span class="hl-punct">)</span>
<span class="hl-punct">}</span>

<span class="hl-kw">val</span> intFunction<span class="hl-punct">:</span> <span class="hl-punct">(</span><span class="hl-type">Int</span><span class="hl-punct">)</span> <span class="hl-punct">-</span><span class="hl-punct">&gt;</span> <span class="hl-type">Int</span> <span class="hl-punct">=</span> <span class="hl-fn">IntTransformer</span><span class="hl-punct">(</span><span class="hl-punct">)</span></code></pre><div><p data-pm-slice="0 0 []">The compiler can infer the function types for variables if there is enough information:</p><p data-pm-slice="0 0 []"><span class="prism--token prism--keyword" style="border: 0px; font-variant-ligatures: none; font-variant-numeric: inherit; font-variant-east-asian: inherit; font-variant-alternates: inherit; font-variant-position: inherit; font-variant-emoji: inherit; font-stretch: inherit; font-size: 15.2px; line-height: inherit; font-family: &quot;JetBrains Mono&quot;, SFMono-Regular, Consolas, &quot;Liberation Mono&quot;, Menlo, Courier, monospace; font-optical-sizing: inherit; font-size-adjust: inherit; font-kerning: inherit; font-feature-settings: inherit; font-variation-settings: inherit; font-language-override: inherit; vertical-align: baseline; color: rgb(0, 51, 179); letter-spacing: 0.024px; white-space: pre; background-color: rgba(25, 25, 28, 0.05);"></span></p><pre class="rte-code" data-lang="kotlin"><code><span class="hl-kw">val</span> a <span class="hl-punct">=</span> <span class="hl-punct">{</span> i<span class="hl-punct">:</span> <span class="hl-type">Int</span> <span class="hl-punct">-</span><span class="hl-punct">&gt;</span> i <span class="hl-punct">+</span> <span class="hl-num">1</span> <span class="hl-punct">}</span> <span class="hl-com">// The inferred type is (Int) -&gt; Int</span></code></pre><div><br></div><div><p data-pm-slice="0 0 []"><a href="https://kotlinlang.org/docs/lambdas.html#invoking-a-function-type-instance" class="ext-link" target="_blank" rel="noopener">Invoking a function type instance</a></p></div><span class="prism--token prism--comment" style="border: 0px; font-variant-ligatures: none; font-variant-numeric: inherit; font-variant-east-asian: inherit; font-variant-alternates: inherit; font-variant-position: inherit; font-variant-emoji: inherit; font-stretch: inherit; font-size: 15.2px; line-height: inherit; font-family: &quot;JetBrains Mono&quot;, SFMono-Regular, Consolas, &quot;Liberation Mono&quot;, Menlo, Courier, monospace; font-optical-sizing: inherit; font-size-adjust: inherit; font-kerning: inherit; font-feature-settings: inherit; font-variation-settings: inherit; font-language-override: inherit; vertical-align: baseline; color: rgb(140, 140, 140); letter-spacing: 0.024px; white-space: pre; background-color: rgba(25, 25, 28, 0.05);"></span><p></p></div><span class="prism--token prism--punctuation" style="border: 0px; font-variant-ligatures: none; font-variant-numeric: inherit; font-variant-east-asian: inherit; font-variant-alternates: inherit; font-variant-position: inherit; font-variant-emoji: inherit; font-stretch: inherit; font-size: 15.2px; line-height: inherit; font-family: &quot;JetBrains Mono&quot;, SFMono-Regular, Consolas, &quot;Liberation Mono&quot;, Menlo, Courier, monospace; font-optical-sizing: inherit; font-size-adjust: inherit; font-kerning: inherit; font-feature-settings: inherit; font-variation-settings: inherit; font-language-override: inherit; vertical-align: baseline; color: rgb(153, 153, 153); letter-spacing: 0.024px; white-space: pre; background-color: rgba(25, 25, 28, 0.05);"></span><p></p></div></div></div><br><div class="rte-menu"><div class="rte-menu-h" data-open="0"><span style="font-size: 18px; font-weight: 700;"><a href="https://kotlinlang.org/docs/inline-functions.html" class="ext-link" target="_blank" rel="noopener">Inline functions</a></span></div><div class="rte-menu-b"><div><br></div><div><p data-pm-slice="0 0 []">Việc dùng <a target="_blank" rel="noopener" href="https://kotlinlang.org/docs/lambdas.html" class="ext-link">higher-order function</a> kéo theo một số chi phí lúc chạy: mỗi function là một object, và it capture a closure. Closure là phạm vi các biến mà thân hàm có thể truy cập được. Việc cấp phát bộ nhớ (cho cả function object lẫn class) cùng với virtual call tạo ra overhead lúc runtime.</p><p data-pm-slice="0 0 []"><br></p></div><div><p data-pm-slice="0 0 []"></p><pre class="rte-code" data-lang="kotlin"><code><span class="hl-com">//Kotlin biên dịch xuống JVM, mà JVM không có "hàm là công dân hạng nhất". </span>
<span class="hl-com">//Nên mỗi lambda bạn truyền vào hàm sẽ bị compiler biến thành </span>
<span class="hl-com">//một object (Function0, Function1...):</span>

<span class="hl-kw">fun</span> <span class="hl-fn">measure</span><span class="hl-punct">(</span>block<span class="hl-punct">:</span> <span class="hl-punct">(</span><span class="hl-punct">)</span> <span class="hl-punct">-</span><span class="hl-punct">&gt;</span> <span class="hl-type">Unit</span><span class="hl-punct">)</span> <span class="hl-punct">{</span>
&nbsp; &nbsp; <span class="hl-kw">val</span> start <span class="hl-punct">=</span> <span class="hl-type">System</span><span class="hl-punct">.</span><span class="hl-fn">nanoTime</span><span class="hl-punct">(</span><span class="hl-punct">)</span>
&nbsp; &nbsp; <span class="hl-fn">block</span><span class="hl-punct">(</span><span class="hl-punct">)</span>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; <span class="hl-com">// thực chất là block.invoke()</span>
&nbsp; &nbsp; <span class="hl-fn">println</span><span class="hl-punct">(</span><span class="hl-type">System</span><span class="hl-punct">.</span><span class="hl-fn">nanoTime</span><span class="hl-punct">(</span><span class="hl-punct">)</span> <span class="hl-punct">-</span> start<span class="hl-punct">)</span>
<span class="hl-punct">}</span>

measure <span class="hl-punct">{</span> <span class="hl-fn">doSomething</span><span class="hl-punct">(</span><span class="hl-punct">)</span> <span class="hl-punct">}</span>&nbsp; &nbsp;<span class="hl-com">// tạo 1 object Function0 mỗi lần gọi</span></code></pre></div><div><p data-pm-slice="0 0 []"><code></code></p><pre class="rte-code" data-lang="kotlin"><code><span class="hl-com">//inline làm gì</span>
<span class="hl-com">//Đánh dấu inline thì compiler chép thân hàm </span>
<span class="hl-com">//VÀ thân lambda thẳng vào chỗ gọi:</span>

<span class="hl-kw">inline</span> <span class="hl-kw">fun</span> <span class="hl-fn">measure</span><span class="hl-punct">(</span>block<span class="hl-punct">:</span> <span class="hl-punct">(</span><span class="hl-punct">)</span> <span class="hl-punct">-</span><span class="hl-punct">&gt;</span> <span class="hl-type">Unit</span><span class="hl-punct">)</span> <span class="hl-punct">{</span> <span class="hl-punct">.</span><span class="hl-punct">.</span><span class="hl-punct">.</span> <span class="hl-punct">}</span>

<span class="hl-com">// Chỗ gọi này:</span>
measure <span class="hl-punct">{</span> <span class="hl-fn">doSomething</span><span class="hl-punct">(</span><span class="hl-punct">)</span> <span class="hl-punct">}</span>

<span class="hl-com">// Được biên dịch thành (đại ý):</span>
<span class="hl-kw">val</span> start <span class="hl-punct">=</span> <span class="hl-type">System</span><span class="hl-punct">.</span><span class="hl-fn">nanoTime</span><span class="hl-punct">(</span><span class="hl-punct">)</span>
<span class="hl-fn">doSomething</span><span class="hl-punct">(</span><span class="hl-punct">)</span>
<span class="hl-fn">println</span><span class="hl-punct">(</span><span class="hl-type">System</span><span class="hl-punct">.</span><span class="hl-fn">nanoTime</span><span class="hl-punct">(</span><span class="hl-punct">)</span> <span class="hl-punct">-</span> start<span class="hl-punct">)</span></code></pre></div><div><br></div><div><div></div><pre class="rte-code" data-lang="kotlin"><code><span class="hl-com">//Có capture. chỉ có biến bên ngoài lambda mới phải capture</span>
<span class="hl-kw">fun</span> <span class="hl-fn">main</span><span class="hl-punct">(</span><span class="hl-punct">)</span> <span class="hl-punct">{</span>
&nbsp; &nbsp; <span class="hl-kw">val</span> userId <span class="hl-punct">=</span> <span class="hl-num">42</span>
&nbsp; &nbsp; <span class="hl-kw">val</span> name <span class="hl-punct">=</span> <span class="hl-str">"tung"</span>

&nbsp; &nbsp; <span class="hl-fn">repeat</span><span class="hl-punct">(</span><span class="hl-num">1_000_000</span><span class="hl-punct">)</span> <span class="hl-punct">{</span>
&nbsp; &nbsp; &nbsp; &nbsp; measure <span class="hl-punct">{</span> <span class="hl-fn">saveUser</span><span class="hl-punct">(</span>userId<span class="hl-punct">,</span> name<span class="hl-punct">)</span> <span class="hl-punct">}</span>&nbsp; &nbsp;<span class="hl-com">// ← capture userId và name</span>
&nbsp; &nbsp; <span class="hl-punct">}</span>
<span class="hl-punct">}</span>

<span class="hl-com">// &lt;=&gt;</span>

<span class="hl-kw">final</span> <span class="hl-kw">class</span> <span class="hl-type">Main$main$1</span> implements <span class="hl-type">Function0</span> <span class="hl-punct">{</span>
    <span class="hl-kw">private</span> <span class="hl-kw">final</span> int userId<span class="hl-punct">;</span>        <span class="hl-com">// ← closure</span>
    <span class="hl-kw">private</span> <span class="hl-kw">final</span> <span class="hl-type">String</span> name<span class="hl-punct">;</span>       <span class="hl-com">// ← closure</span>

    <span class="hl-fn">Main$main$1</span><span class="hl-punct">(</span>int userId<span class="hl-punct">,</span> <span class="hl-type">String</span> name<span class="hl-punct">)</span> <span class="hl-punct">{</span>
        <span class="hl-kw">this</span><span class="hl-punct">.</span>userId <span class="hl-punct">=</span> userId<span class="hl-punct">;</span>
        <span class="hl-kw">this</span><span class="hl-punct">.</span>name <span class="hl-punct">=</span> name<span class="hl-punct">;</span>
    <span class="hl-punct">}</span>
    <span class="hl-kw">public</span> void <span class="hl-fn">invoke</span><span class="hl-punct">(</span><span class="hl-punct">)</span> <span class="hl-punct">{</span> <span class="hl-fn">saveUser</span><span class="hl-punct">(</span>userId<span class="hl-punct">,</span> name<span class="hl-punct">)</span><span class="hl-punct">;</span> <span class="hl-punct">}</span>
<span class="hl-punct">}</span>

<span class="hl-com">// Mỗi vòng lặp:</span>
<span class="hl-fn">measure</span><span class="hl-punct">(</span>new <span class="hl-fn">Main$main$1</span><span class="hl-punct">(</span>userId<span class="hl-punct">,</span> name<span class="hl-punct">)</span><span class="hl-punct">)</span><span class="hl-punct">;</span>   <span class="hl-com">// ← new 1 object, 1 triệu lần</span></code></pre><div><br></div><div><p data-pm-slice="0 0 []"><a href="https://kotlinlang.org/docs/inline-functions.html#reified-type-parameters" class="ext-link" target="_blank" rel="noopener">Reified type parameters﻿</a></p><p data-pm-slice="0 0 []">Đôi khi bạn cần biết <code>T</code> thực sự là kiểu gì ngay trong thân hàm — và đây là cách làm được điều đó</p><pre class="rte-code" data-lang="kotlin" style="color: rgb(188, 190, 196);"><code><span class="hl-kw">private</span> <span class="hl-kw">inline</span> <span class="hl-kw">fun</span> <span class="hl-punct">&lt;</span><span class="hl-kw">reified</span> <span class="hl-type">T</span> <span class="hl-punct">:</span> <span class="hl-type">Any</span><span class="hl-punct">&gt;</span> <span class="hl-fn">convertJsonToList</span><span class="hl-punct">(</span>json<span class="hl-punct">:</span> <span class="hl-type">String</span><span class="hl-punct">)</span><span class="hl-punct">:</span> <span class="hl-type">List</span><span class="hl-punct">&lt;</span><span class="hl-type">T</span><span class="hl-punct">&gt;</span> <span class="hl-punct">{</span>
    <span class="hl-kw">val</span> moshi<span class="hl-punct">:</span> <span class="hl-type">Moshi</span> <span class="hl-punct">=</span> <span class="hl-type">Moshi</span><span class="hl-punct">.</span><span class="hl-fn">Builder</span><span class="hl-punct">(</span><span class="hl-punct">)</span><span class="hl-punct">.</span><span class="hl-fn">add</span><span class="hl-punct">(</span><span class="hl-fn">KotlinJsonAdapterFactory</span><span class="hl-punct">(</span><span class="hl-punct">)</span><span class="hl-punct">)</span><span class="hl-punct">.</span><span class="hl-fn">build</span><span class="hl-punct">(</span><span class="hl-punct">)</span>
    <span class="hl-kw">val</span> jsonAdapter<span class="hl-punct">:</span> <span class="hl-type">JsonAdapter</span><span class="hl-punct">&lt;</span><span class="hl-type">List</span><span class="hl-punct">&lt;</span><span class="hl-type">T</span><span class="hl-punct">&gt;</span><span class="hl-punct">&gt;</span> <span class="hl-punct">=</span>
        moshi<span class="hl-punct">.</span><span class="hl-fn">adapter</span><span class="hl-punct">(</span><span class="hl-type">Types</span><span class="hl-punct">.</span><span class="hl-fn">newParameterizedType</span><span class="hl-punct">(</span><span class="hl-type">List</span><span class="hl-punct">:</span><span class="hl-punct">:</span><span class="hl-kw">class</span><span class="hl-punct">.</span>java<span class="hl-punct">,</span> <span class="hl-type">T</span><span class="hl-punct">:</span><span class="hl-punct">:</span><span class="hl-kw">class</span><span class="hl-punct">.</span>java<span class="hl-punct">)</span><span class="hl-punct">)</span>
    <span class="hl-kw">return</span> jsonAdapter<span class="hl-punct">.</span><span class="hl-fn">fromJson</span><span class="hl-punct">(</span>json<span class="hl-punct">)</span> <span class="hl-punct">?</span><span class="hl-punct">:</span> <span class="hl-fn">emptyList</span><span class="hl-punct">(</span><span class="hl-punct">)</span>
<span class="hl-punct">}</span></code></pre></div><div><br></div><div></div></div></div></div><br><br><div class="rte-menu"><div class="rte-menu-h" data-open="1"><span style="font-size: 18px; font-weight: 700;"><a href="https://kotlinlang.org/docs/scope-functions.html" class="ext-link" target="_blank" rel="noopener">Scope functions</a></span></div><div class="rte-menu-b"></div></div><br><br></div><div><div><br></div><div><span style="font-weight: bold;"><span style="font-size: 20px;"><span style="font-size: 15px;"><span style="font-size: 18px;">Variables</span></span></span></span><br><a href="https://kotlinlang.org/docs/basic-syntax.html#variables" class="ext-link">https://kotlinlang.org/docs/basic-syntax.html#variables</a></div></div>

